apiVersion: apps/v1
kind: Deployment
metadata:
  name: devstack
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: nodejs
  template:
    metadata:
      labels:
        app: nodejs
    spec:
      containers:
      - image: vmsalem/nodejs2:b2.20200528.082920
        name: nodejs
        ports:
        - containerPort: 8000      
